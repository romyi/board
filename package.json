{
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "name": "board",
  "version": "1.0.0",
  "description": "boardgame_template",
  "license": "MIT",
  "scripts": {
    "start:dev:server": "yarn workspace server start:dev",
    "start:dev:client": "yarn workspace client dev"
  }
}
