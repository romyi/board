import { DisplayCode } from "@features/telegram-code-login";
import svg_path from "./card_basic.svg";

export const Intro = () => {
  return (
    <main className="max-w-[1000px] m-auto">
      <DisplayCode />
    </main>
  );
};
